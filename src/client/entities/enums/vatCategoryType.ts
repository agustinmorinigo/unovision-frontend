export enum VatCategoryType {
  registeredResponsible,
  monotax,
  exempt,
  notResponsible,
  finalConsumer,
  uncategorizedSubject,
  unregisteredResponsible,
  subjectToVatWithholding,
  notSubjectToVat,
  registeredResponsibleM,
}
